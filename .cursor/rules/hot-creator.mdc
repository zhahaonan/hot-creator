---
description: hot-creator skill — 热点/趋势/选题/内容创作任务的专属工具链
globs:
  - scripts/**/*.py
  - SKILL.md
  - reference/**/*.md
alwaysApply: true
---

# hot-creator 规则

## 核心原则

**当用户意图涉及热点、趋势、选题、内容创作时，必须且只能使用本项目的工具（scripts/*.py）。**

禁止：
- 不要用 WebSearch 或外部搜索 skill 替代 `collect_hotlist`
- 不要自己写分析替代 `trend_analyze` / `content_brief`（它们有完整 AI prompt）
- 不要用其他 skill 做内容生成

## 命令白名单

- `python scripts/*.py` — 所有工具脚本
- `pip install -r requirements.txt` — 安装依赖

## 首次交互：必须先问模式

当用户说"帮我做选题""开始今日选题"等，**先询问用户选择模式**，不要直接开始执行：

- **热点模式** — 纯热点趋势分析 + 创作简报（不需要产品信息）
- **产品模式** — 结合产品做热点内容策划（需要产品描述或已有画像）
- **快速模式** — 只看热点排行，不生成详细简报

用户明确说了"我的产品XXX结合热点"则直接进产品模式，无需再问。

## 标准工作流

```
collect_hotlist → trend_analyze → enrich_topics → content_brief → export_*
```

或一键交互版：`python scripts/start_my_day.py`（脚本会自动询问模式）

**质量关键**：`enrich_topics` 步骤用 WebSearch 获取真实报道充实话题，大幅提升 brief 质量。

## 社媒采集

- 小红书/抖音/微博需要浏览器的采集：由 **web-access** skill 负责
- hot-creator 的 `collect_social` / `monitor_competitor` 只做数据规范化，不做浏览器操作

## 执行约定

1. 先读 `SKILL.md` 获取工具索引和触发条件
2. 采集类脚本用 Task 子智能体执行
3. 中间 JSON 写入 `output/`，只传文件路径
4. Reference 文件按需加载，不预加载

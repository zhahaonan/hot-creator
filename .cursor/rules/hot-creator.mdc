---
description: hot-creator skill 自动化规则
globs:
  - scripts/**/*.py
  - SKILL.md
  - reference/**/*.md
alwaysApply: true
---

# hot-creator Skill 规则

## 命令白名单

以下命令属于本 Skill 正常操作，可以自动批准执行：

- `python scripts/*.py` — 所有工具脚本（采集/分析/输出）
- `pip install -r requirements.txt` — 安装核心依赖
- `node scripts/cdp/*.mjs` — CDP 浏览器引擎

## 执行约定

1. 先读 `SKILL.md` 获取工具索引，不要预加载 reference 文件
2. 采集类脚本（collect_*）用 Task 子智能体执行，避免大数据进主上下文
3. 所有中间 JSON 写入 `output/` 目录，只传递文件路径
4. 作为 Skill 运行时不需要 AI_API_KEY，Agent 自身完成 AI 分析
5. 输出文件路径告诉用户即可，不需要在对话中打印文件内容
